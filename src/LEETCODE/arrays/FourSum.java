package LEETCODE.arrays;

import java.util.ArrayList;
import java.util.List;

/**
 * Given an array nums of n integers and an integer target, are there elements a, b, c, and d in nums such that a + b + c + d = target? Find all unique quadruplets in the array which gives the sum of target.
 *
 * Notice that the solution set must not contain duplicate quadruplets.
 *
 *
 *
 * Example 1:
 *
 * Input: nums = [1,0,-1,0,-2,2], target = 0
 * Output: [[-2,-1,1,2],[-2,0,0,2],[-1,0,0,1]]
 * Example 2:
 *
 * Input: nums = [], target = 0
 * Output: []
 */
public class FourSum {
    public static void main(String[] args) {

    }

//    private List<List<Integer>> fourSum(int[] nums, int target) {
//
//    }

//    private List<List<Integer>> fourSum(int[] nums, int index, int sum, int target, List<List<Integer>> result, ArrayList<Integer> innerList) {
//        if(sum == target && innerList.size() == 4){
//            result.add(innerList);
//            return result;
//        }
//
//        for (int i = index; i < nums.length ; i++) {
//            int currentSum = sum+nums[i];
//            if(currentSum > target) break;
//
//
//        }
//        return
//    }
}
